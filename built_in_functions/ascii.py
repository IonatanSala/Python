# ascii(object)
# this returns a string containing a printible representation
# of an object, but escape the non-ASCII charcters in the string returned by repr()
# using \x, \u or \U escapes.

# Come to this later